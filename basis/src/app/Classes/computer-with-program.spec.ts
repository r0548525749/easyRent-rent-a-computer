import { ComputerWithProgram } from './computer-with-program';

describe('ComputerWithProgram', () => {
  it('should create an instance', () => {
    expect(new ComputerWithProgram()).toBeTruthy();
  });
});
